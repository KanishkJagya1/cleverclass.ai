[pytest]
testpaths = tests

# NOTE: temp-directory redirection lives in tests/conftest.py, not here.
# `env = ...` in this file requires the pytest-env plugin; without it pytest
# prints "Unknown config option: env" as a warning and silently ignores the
# setting, which is how scratch kept landing on a full C: drive while the
# config looked correct.
