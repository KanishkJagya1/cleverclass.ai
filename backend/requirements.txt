# --- Core -------------------------------------------------------------------
fastapi==0.115.8
uvicorn[standard]==0.34.0
pydantic==2.10.6
pydantic-settings==2.7.1
python-multipart==0.0.20
# EmailStr, used by the admin and order schemas.
email-validator==2.2.0

# --- Vector store -----------------------------------------------------------
chromadb==0.6.3

# --- Embeddings -------------------------------------------------------------
# ~90 MB model, cached on a MOUNTED volume rather than baked into the image.
# Pulls torch: roughly 2.2 GB of layers and ~500 MB resident. Kept local by an
# explicit decision (no per-query network call, no per-query cost), so the
# container carries a hard memory limit and the deploy script checks headroom
# before building.
sentence-transformers==3.4.1

# --- PDF --------------------------------------------------------------------
# fitz. Renders pages, which pypdf cannot, and extracts Devanagari markedly
# better — most of this catalogue is set in it.
#
# LICENCE: AGPL-3.0, and this service is network-served, so §13 applies. The LMS
# stack on the same host already carries the same exposure. poppler-utils
# (pdftoppm / pdftotext, GPL-2, invoked as a subprocess with no linking) is the
# escape hatch and would change only app/services/pdf.py.
PyMuPDF==1.25.5

# --- LLM providers ----------------------------------------------------------
# Gemini is called over plain HTTPS from app/services/llm.py using the same key
# as the LMS stack.
openai==1.63.2

# REMOVED, deliberately:
#   pypdf                 the only remaining PDF reader is app/services/pdf.py.
#                         The directory walker that used pypdf to index
#                         arbitrary PDFs into the corpus is gone — it was the
#                         leak the free-page feature exists to prevent.
#   google-generativeai   deprecated SDK, pulls grpc, ~40 MB for an HTTP POST.
